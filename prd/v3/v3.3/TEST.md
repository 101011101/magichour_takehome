# v3.3 — TEST

**Status: link 1 matrix as written; later links reuse the same 28 references with the arms and budgets recorded in RESULTS (§2, §3, §5, §6), which is where phase-2 set-up was written before its run.**

The matrix is **the run-B fold, unchanged**: `v3/testsets/v30_matrix_b.csv`, 28 pairs,
28 distinct garment references, generated by `v3/build/make_matrix_b.py` and documented
in [v3.0 TEST.md](../v3.0/TEST.md). Reusing it is the point: every arm already on disk
for these pairs (`BC`, `QX`, `MQ`, `PH`, `PH2`) is comparable to `MK` without argument
about the set.

## Held fixed

| | |
|---|---|
| input | `v3/runs/v3.0b/inputs/{garment}__A4.jpg` — the crops v3.1 link 10 generated, not recomputed. **Links 1.3–1.4 deviated**: klein on the raw frame, crop after ([RESULTS §5.5](RESULTS.md#55-reviewers-notes-on-45-recorded-2026-08-29)); phase 2 restores crop-first |
| person for the colour read | `v3/runs/v3.0b/inputs/{person}.jpg` |
| readers | `v3/colab/lib/v3lib.py` `tone()` and `framing()`, weights in `v2/runs/.models` |
| prompt | `v3lib.mq_prompt()` — `PREFIX + colour + SUFFIX + FRAME_CLAUSE[framing]` |
| seed | 46 |
| call 1 | `fal-ai/flux-2/klein/4b/distilled/edit`, one image in |
| budget | link 1: 28 calls. Spent through phase 2: 28 + 32 + 124 + 28 + 112 + 209 klein calls. link 2: 56 |

## Review protocol

Link 1 is a visual gate, one reviewer, unblinded, against the five checks in
[EXPERIMENT.md §1](EXPERIMENT.md#1--the-reference-only-crop--klein-mannequin--this-run).
The Qwen column on the page is orientation only — it predates dynamic prompting.
